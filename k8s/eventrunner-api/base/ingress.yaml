apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eventrunner-api
  namespace: eventrunner
  annotations:
    cert-manager.io/cluster-issuer: threadr-issuer
    nginx.ingress.kubernetes.io/auth-url: "http://oathkeeper-proxy.oathkeeper.svc.cluster.local:4455/decisions"
    nginx.ingress.kubernetes.io/auth-response-headers: "X-User,X-Tenant-ID"
    nginx.ingress.kubernetes.io/auth-snippet: |
      proxy_set_header Authorization $http_authorization;
      proxy_set_header X-Original-URI $request_uri;
      proxy_set_header X-Original-Method $request_method;
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - api.tunnel.threadr.ai
      secretName: eventrunner-api-tls
  rules:
    - host: api.tunnel.threadr.ai
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: eventrunner-api
                port:
                  number: 8200